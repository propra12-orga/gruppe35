package main;

import java.util.ArrayList;
import Character.Enemy;

/**
 * 
 * The Enemylist class keeps track of all the Enemy which are currently present.
 * Its only purpose is to provide the GUI with their coordinates to draw them
 * <P>
 * 
 * @author Peet
 */

public class Enemylist {
	public static ArrayList<Enemy> list = new ArrayList<Enemy>();
}
